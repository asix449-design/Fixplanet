---
import { isGeoShelf } from '../data/terraforming';

const shelf = Astro.url.searchParams.get('shelf');
const dest = isGeoShelf(shelf ?? undefined) ? `/terraforming/${shelf}` : '/terraforming';
return Astro.redirect(dest, 301);
---
